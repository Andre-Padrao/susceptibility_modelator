[general]
name=Susceptibility Modelator
qgisMinimumVersion=3.0
description=Produces and validates susceptibility models for various risk factors
version=1.0
author=André Padrão
email=apadrao@floradata.pt

about=Teste.This QGIS plugin facilitates the production and evaluation of susceptibility models for any type of spatially distributed risk. It is associated to the paper "A GIS plugin for wildfire susceptibility modeling: case study in Vila Nova de Foz Côa", by André Padrão et al., where one can find further details on its development and use.

# tracker=http://bugs
repository=https://github.com/Andre-Padrao/susceptibility_modelator.git
# End of mandatory metadata

# Recommended items:

hasProcessingProvider=no
# Uncomment the following line and add your changelog:
# changelog=

# Tags are comma separated with spaces allowed
tags=susceptibility modeling,python,spatial planning,environmental variables

# homepage=https://github.com/Andre-Padrao/susceptibility_modelator.git
category=Plugins
icon=icon.png
# experimental flag
experimental=True

# deprecated flag (applies to the whole plugin, not just a single version)
deprecated=False

# Since QGIS 3.8, a comma separated list of plugins to be installed
# (or upgraded) can be specified.
# Check the documentation for more information.
# plugin_dependencies=pandas,matplotlib,numpy

Category of the plugin: Raster, Vector, Database or Web
# category=

# If the plugin can run on QGIS Server.
server=False

